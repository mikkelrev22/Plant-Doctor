[tool.coverage.run]
branch = true
source = [ "src" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../coverage/apps/backend-agent/html'
 --cov-report xml:'../../coverage/apps/backend-agent/coverage.xml'
 --html='../../reports/apps/backend-agent/unittests/html/index.html'
 --junitxml='../../reports/apps/backend-agent/unittests/junit.xml'
"""

[project]
name = "backend-agent"
version = "0.1.0"
description = "Plant Doctor LangGraph agent service — reference draft that wires the Node /agent tool gateway and streams the AI SDK UI Message Stream protocol."
requires-python = ">=3.11,<3.14"
readme = 'README.md'
dependencies = [
    "fastapi[standard]>=0.138.2",
    "httpx>=0.27.0",
    "langchain-core>=0.3.0",
    "langchain-openai>=0.3.0",
    "langgraph>=0.4.0",
    "python-dotenv>=1.2.2",
]


[dependency-groups]
dev = [
  "autopep8>=2.3.1",
  "ruff>=0.8.2",
  "pytest>=8.3.4",
  "pytest-sugar>=1.0.0",
  "pytest-cov>=6.0.0",
  "pytest-html>=4.1.1",
]

[build-system]
requires = ["uv_build>=0.8.9,<0.9.0"]
build-backend = "uv_build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []
fixable = ["ALL"]
unfixable = []